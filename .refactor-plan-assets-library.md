# assets/library.rs 提取计划

## 目标
从 `assets.rs` (4114行) 提取素材库查询模块到 `assets/library.rs`，预计减少 ~1000-1200 行。

## 提取范围

### 1. 常量 (从 assets.rs 复制)
- `ASSET_PAGE_FILTER_SQL` (line 94-119)

### 2. 辅助函数 (纯函数，无副作用)
- `asset_safe_directory` (line 2868-2881) - 路径脱敏
- `asset_public_folder_metadata` (line 2849-2866) - 目录元数据
- `safe_legacy_parent_parts` (line 2883-2898)
- `legacy_source_parent` (line 2900-2945)
- `legacy_drive_parent` (需要查找)
- `asset_directory_nodes` (line 3047-3087) - 目录树构建

### 3. 核心查询函数
- `project_asset_directories` (line 3010-3045) - 项目目录投影
- `list_assets_snapshot` (line 3347-3428) - 快照查询
- `list_assets` (line 3335-3345) - 简化列表 (Tauri命令)
- `list_asset_page` (line 3430-3612) - 分页查询 (Tauri命令)
- `get_asset_evidence` (line 3615-3771) - 证据查询 (Tauri命令)

### 4. Collection/Tag/Metadata 查询 (Tauri命令)
- `list_asset_collections` (line 2120-2142)
- `create_asset_collection` (line 2098-2118)
- `add_assets_to_collection` (line 2144-2192)
- `update_asset_user_metadata_batch` (line 1986-2033)
- `add_asset_tag_batch` (line 2035-2070)
- `remove_asset_tag_batch` (line 2072-2096)

### 5. 依赖问题处理

**需要保留在 assets.rs 的函数：**
- `drain_pending_analysis` - 分析模块的副作用，只在 list_asset_page 末尾调用
- `derived_directory` - 被分析模块大量使用

**解决方案：**
- library.rs 通过 `pub(crate)` 暴露给父模块
- assets.rs 保留 Tauri 命令包装，转发给 library.rs
- `drain_pending_analysis` 调用保留在 assets.rs 的转发层

## 执行步骤

### Phase 1: 创建模块骨架
1. 创建 `src-tauri/src/assets/library.rs`
2. 创建 `src-tauri/src/assets/mod.rs`
3. 在 `lib.rs` 中注册新模块

### Phase 2: 提取纯函数 (无外部依赖)
4. 复制 `ASSET_PAGE_FILTER_SQL`
5. 复制路径辅助函数 (asset_safe_directory 等)
6. 复制目录树函数 (asset_directory_nodes)

### Phase 3: 提取查询函数 (依赖 db/models)
7. 提取 `project_asset_directories`
8. 提取 `list_assets_snapshot`
9. 提取 `list_asset_page` 核心逻辑
10. 提取 `get_asset_evidence`

### Phase 4: 提取 Collection/Tag/Metadata
11. 提取 collection 相关函数
12. 提取 tag 相关函数
13. 提取 metadata 相关函数

### Phase 5: 重构 assets.rs
14. assets.rs 保留 Tauri 命令声明
15. 命令实现转发给 library 模块
16. 处理 `drain_pending_analysis` 调用

### Phase 6: 验证
17. `cargo check`
18. `cargo test`
19. `npm run harness:check`
20. 真实桌面验证

## 预期结果
- `assets/library.rs`: ~1000-1200 行
- `assets.rs`: ~2900-3100 行 (减少 ~1000 行)
- 所有测试通过
- Tauri 命令对前端透明（无破坏性变更）
