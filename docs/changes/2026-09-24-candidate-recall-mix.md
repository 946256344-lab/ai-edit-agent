# 候选召回比例与 9 条可选

每拍候选池仍最多 9 条。项目设置中的“综合分优先数量”默认 5，可选 3～9；前几条按原综合分排序入池，余下名额依次从 CLIP 画面、语义、关键词三个分项的高分候选补入，沿用同素材最多两段和相似画面最多两条的限制。分项无可用候选时按综合分补足。

Phase 3 已接收 9 条候选网格，现在提示词与解析都允许选择池中任意序号。设置保存在项目 `settings_json.candidateScoreFirstSlots`，新生成的分镜和手动刷新替换建议读取该值；已有故事板不改写。

同步文档：`docs/api.md`。
