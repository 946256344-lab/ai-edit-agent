# 异步源文件健康扫描

- schema v13 新增独立的 `asset_source_health` 快照。
- 新增启动、取消和汇总命令；扫描以 `agent_tasks.scan_asset_health` 审计，且只读取文件元数据。
- 素材列表返回持久化健康状态，不在浏览路径执行文件系统检查。
- 新导入和确认重链路建立新基线；已有素材首次扫描以当前可读文件建立基线。
- 素材面板增加健康汇总、检查和取消操作。
