# 2026-09-18：卡片让位于网格与 CLIP

第一次分析卡片会写错。不重跑分析；选片改信图，精修不再把整片另切窗。

## 改动

- Phase 2：CLIP 查询可用但候选没有片段图向量时，词面/语义各最多 5 分，且不再靠「有卡片」排到带图向量的段前面，避免整片假卡进前 9。
- Phase 3：提示以网格为准；卡片与画面冲突时看图。整片候选的源窗写成全片，供 Phase 4 锁定。
- Phase 4：没有 `segmentId` 也锁在 P3 源窗，跳过 Pass A 按关键帧另切窗；Pass B 仍在该窗内精修。

公开命令不变。CLIP 模型缺失时词面路径与原来一样。
