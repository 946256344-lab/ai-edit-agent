# 浅紫粗剪工作台

日期：2026-09-13。实现分支：`codex/lavender-workspace`。

## 改动

按用户确认的参考图实现浅紫磨砂外框、衬线标题与连续白色画布，进一步调整正文对比度、字号、侧栏宽度和播放控件。
- 素材库位于左侧当前项目下方；会话常驻侧栏，顶部保留返回粗剪入口。
- 左侧开放式对话、底部固定输入框；右侧真实预览、镜头条、替换与撤销/重做。
- 剪映交付入口移至右上，沿用现有未保存修改处理。
- 播放器提供播放/暂停、进度、静音和全屏；横竖媒体保持原比例。
- 会话封面与镜头缩略图读取真实源关键帧。镜头条按源区间取帧，不依赖素材库目录或分页；区间没有可用帧时显示镜头图标。
- 素材库与设置弹窗统一浅紫配色；更新已有桌面烟雾检查的导航与原生 dialog 选择器。
- 原有模型文件和 prototypes 目录未改动。无新增依赖、Tauri 命令或数据库变更。

## 验证

- lint、build、architecture:check、branch:check、git diff --check 通过；lint 保留既有 StudioWorkspace.tsx:525 警告。
- 真实 Tauri WebView 烟雾通过：素材库/粗剪切换、模型设置打开关闭，无运行时错误。
- 已有 540×960 本地预览能播放；验证暂停、镜头定位、静音和全屏。
- 检查 1586×992、1440×900、1024×650 与 390×844 CSS 布局；无页面横向溢出，1024×650 输入与候选保存按钮可见。
- 已有 12 个候选的列表正常显示并独立滚动。部分候选源素材不可用；未调用模型重生成、未保存替换、未导出。
- harness:check 仍被原有 outbound_http.rs 网络白名单检查阻断；harness:test 仍被原有文档同步测试断言阻断。与 2026-09-12 frontend-polish 记录一致。

实现已同步到原项目供开发版使用；未提交、推送或重新打包安装程序。
