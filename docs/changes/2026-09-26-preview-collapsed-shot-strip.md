# 2026-09-26：预览面板收起镜头条

## 结果

- 视频预览下方的镜头缩略图条默认收起，画面舞台占满面板高度，布局与未开始剪辑时的空状态一致。
- 舞台下只保留一行：左侧胶片图标加镜头计数（如 `04/06`），点它展开或收起镜头条，展开时计数变为淡紫底；悬停提示显示“当前镜头 04 · 2.7 秒 · 展开镜头条”。右侧依次为替换当前镜头、撤销、重做、更新预览。
- “更新预览”由文字按钮改为刷新图标（带提示）；预览落后于时间线时图标变为淡紫底提醒。
- 展开状态只存在组件内，切换会话或重启后回到收起；展开时仍会自动滚到当前镜头。

## 触发范围

- `src/components/RoughCutPreview.tsx`、`src/components/WorkspaceIcon.tsx`（新增 `refresh`）、`src/styles/preview.css`。
- `src/lib/i18n/zh-CN.ts`、`en.ts`：新增 `preview.showShots` / `preview.hideShots`。

## 验证

- `npx tsc -p tsconfig.app.json --noEmit`、`npm run lint`、`npm run i18n:check`：通过。
- 临时页面（已删除）用假数据单独渲染预览面板，330px 宽度下核对：收起时一行放下计数与四个操作、展开后 6 格镜头条出现、预览过期时刷新图标高亮、空状态不变。
- 待真实桌面验收：有缩略图的真实项目中展开、点镜头跳转。
