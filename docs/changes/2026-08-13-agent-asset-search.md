# Agent 素材检索工具

- Agent 循环增加只读 `search_assets`，替代将整个素材库交给模型的做法。
- 支持查询、媒体类型、时长、评分、收藏、标签、集合和分页游标；单页最多 20 条。
- 禁止使用素材始终排除，结果只包含安全摘要及固定命中原因码。
- 不返回路径、用户备注、OCR 正文、媒体内容或完整证据。
- 定向测试覆盖结果上限、项目作用域、禁止使用门和敏感文本脱敏。
- 未启动或重启桌面程序。
