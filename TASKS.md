# 任务清单

## 当前任务窗口

<!-- ACTIVE_TASKS_START -->
- [x] 已实现（2026-09-26，待真实 BGM 验收）：补 Jamendo Client ID 配置入口与明确的 API 失败反馈；官方公共测试 ID 已返回应用暂停，真实搜索、下载、写轨和预览需要可用的自有 Client ID。
- [ ] 进行中（2026-09-26）：10 月 1 日首发准备，已定海外优先。待拍板：发布形态、试用额度与付费、上游模型成本、配音、交付物口径。09-26 已完成代码侧：默认编辑器 CapCut、网关错误码与英文文案、隐藏 OAuth、登录框网站链接、版本请求头、Release CSP、构建前校验网关地址、Premiere 标签；网站仓库本地已提交上游错误映射与总开关（未推送、未部署）。P0 剩余：用正式域名构建、网站推送与生产部署、按 uid 额度（待 D3）、经网关的 413 实测、CapCut 与 Resolve 实测交付、英文界面主路径验收、代码签名、英文 Terms/Privacy 与删除渠道、第三方许可。逐项见 docs/release-checklist.md。
- [x] 已修复（2026-09-26，待桌面确认）：新会话第一次生成，预览显示 v7。原因是故事版和时间线的版本号按项目累加。现在改为按会话（剪辑任务）编号，每个会话从 v1 开始，局部编辑的派生版本同规则。迁移只追加 `task_version_number` 列，不改历史编号：旧会话接着原编号往后，新会话从 v1 开始。排查了快照、状态查询、产物面板、预览缓存、剪映 / CapCut 草稿，没有发现跨会话串用。选镜「新鲜度」经确认保持项目级：同项目各会话用过的素材继续降权，避免多条成片反复用同一批镜头。
- [ ] 待桌面验收（2026-09-26 汇总）：09-24 至 09-26 已写完代码、只差真实桌面确认的 21 项改动（界面改版、双语、发送响应、选镜与故事版、编辑器交付、账号与网关），随首发主路径验收逐条确认；不通过的转成待修项。逐条见 docs/release-checklist.md 附录 A。
- [x] 已修复（2026-09-26，待桌面确认）：审片「测试1」最新预览发现三处阻塞：英文对齐字幕单词间空格全丢（Fish 按词返回时间戳、拼接不加空格）；每 24 字符硬断行导致单词被拦腰截断且超出竖屏宽度；`search_music` 失败、时间线无音乐轨，Agent 仍回复「Background track added」。改为逐词拼接补空格、拉丁文字按词换行（每行 20 字符、最多两行）；`generate_storyboard` 返回 `appliedMedia` / `mediaNotApplied`，系统提示只准按实际落地汇报。旧时间线字幕需重新生成。
- [x] 已修复（2026-09-26，待桌面确认）：同次审片的选镜问题。召回几乎不分拍（词面把 at/in/or 按子串算命中、各拍关键词都带 factory floor/machinery、CLIP 分只差 1–3 分）；上一拍已选的片段仍留在下一拍候选里，模型重复选中后两拍一起被打回，最后换成展会机械臂和画质最低的镜头。改为词面去虚词、按词首匹配、多数拍共用词降权，CLIP 按本拍全库相对换算；进入选片前剔除已用/相似/超复用上限的候选，撞车只打回靠后镜头。「exacting standards」上半白墙是原片构图（机器白色护罩），竖屏裁切已用满高度，非裁切问题。
- [ ] 待办（2026-09-26）：同次审片的非阻塞项。视觉分析缺「画面文字/品牌标识/人群/展会」标记，Tesseract 只带英文数据识别不到中文招牌，海外成片无法避开这类画面（需重跑视觉分析）。「future of industry」库里缺相应画面。`generate_storyboard` 单次约 408 秒。库里有 2 条本地音频，Jamendo 未配置时 Agent 未尝试用本地音频配乐。
- [x] 已修复（2026-09-26，待桌面确认）：时长冲突暂停后用户选「保留口播时长」，Agent 不调工具直接回「Done、草稿已创建」，实际什么都没生成。原因是跨轮历史只有文字，上一轮的稿子和暂停状态下一轮都看不到。改为暂停时稿子存为任务 brief，快照写明暂停于哪个工具，系统提示要求按用户回答续跑、不得声称未返回的产物。
- [x] 已修复（2026-09-26，待桌面确认）：冷启动 `initialize_local_store` 卡约 11 秒。原因是启动时同步整文件哈希约 700 MB 的本地选镜模型，且开发态 StrictMode 调用两次、两份哈希并发。改为哈希移到后台线程并串行复用结果；启动恢复按进程只跑一次，webview 重载不再把运行中的 Agent 任务改判为中断。
- [x] 已修复（2026-09-26，待桌面确认）：配音时长与用户要求冲突时，Agent 扩写太少（30 秒只加一句，19.4 秒→21.2 秒），连续两次被拦。冲突信息改为附带当前稿长和按本次实测语速换算的目标稿长，Agent 按目标长度改稿。
- [x] 已修复（2026-09-26）：剪映「重新检测」点击无反应。重测结果不变时界面毫无反馈（剪映新建草稿后约 15 秒才写回 `root_meta_info.json`）。改为只读重测，显示「检测中…」与「仍未检测到」原因，切回应用窗口时静默重测；检测失败时日志记录卡在哪一步。重启开发版后用户确认可交付；旧进程为何一直检测不到未能事后复现。
- [x] 已删除（2026-09-26）：Phase 3 增量重跑（`prior_shots`）从未生效，且复用分支会丢失旁白、让「重新生成」静默沿用旧镜头。删除该路径，修复轮仍只重跑修复包点名的拍，局部改镜由 `reselect_shots` 承担。
- [x] 已修复（2026-09-26）：素材库显示 95 条、Agent 健康摘要统计 102 条（含 7 条已从素材库移除）。健康扫描、健康摘要和 Agent 状态快照改为与素材库同一口径，排除已移除素材；回归测试覆盖快照与健康摘要。
<!-- ACTIVE_TASKS_END -->

## 说明

这里记录的是近期工作状态，不要求每个新任务都同步到长期文档。遇到问题时优先记录复现方式、实际错误和影响，再决定是修代码、补测试还是暂时接受限制。历史实现可以通过 Git 提交记录查看。
